version: "3.9"

services:
 mysql:
    image: mysql:latest
    container_name: mysql_con
    environment:
      MYSQL_DATABASE: "devops"
      MYSQL_ROOT_PASSWORD: "iram143"
    ports:
      - "3306:3306"
    volumes:  
      - mysql_data:/var/lib/mysql
    networks:
      - todo-net

# 👇 Declare volume & Network
volumes:
  mysql_data:

# 👇 ALSO ADD THIS (warna network error aayega)
networks:
  todo-net:
